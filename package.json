{
  "name": "mycelium-tools",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "worker-build --release"
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
